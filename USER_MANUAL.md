# NH Estimator — User Manual

The manual lives at **[docs/user-manual/README.md](docs/user-manual/README.md)** —
a screen-by-screen illustrated guide, also available as a
[PDF](docs/user-manual/NH-Estimator-User-Manual.pdf) (regenerate with
`npm run build:manual` after editing).

This file is just the signpost: parallel work-in-progress sessions keep
recreating a text-only manual here because they don't know about the
illustrated one. If you're reading this after another rewrite, please
fold any genuinely new content into `docs/user-manual/README.md` instead
and leave this as a pointer — the project keeps exactly one manual.
