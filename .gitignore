# macOS
.DS_Store

# Dependencies
node_modules/

# Secrets. XERO_CLIENT_SECRET, XERO_CLIENT_ID and DATABASE_URL live here
# (see README) -- never commit one. The negation keeps a checked-in
# .env.example possible without it being swallowed by the .env.* rule.
.env
.env.*
!.env.example
